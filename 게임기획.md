# Creature-Tower 게임 기획 / 코드 구조 메모
최종 업데이트: 2026-03-13

---

## 장르 및 기본 구조

- 2D 탑뷰 싱글플레이 로그라이트 보스 러쉬 슈터
- 1스테이지씩 보스를 처치하며 타워를 등반하는 구조

> `MainGameManager` — 기존(멀티) 프로젝트 코드. 참고/이관용으로 보존  
> `GameManager` — 현재 메인 게임 플롯 관리자 (실제 사용 중)

---

## 게임 플로우

```
Init → UIPlaying → Start → Playing → End → AugmentListing → (반복)
```

| 상태 | 설명 |
|---|---|
| UIPlaying | 스테이지 시작 전 캐릭터 선택 / 준비 UI |
| Start | 스테이지 시작, 몬스터 스폰 |
| Playing | 실제 전투 (currentMonsterCount == 0 이 되면 End로) |
| End | 스테이지 클리어 판정 |
| AugmentListing | 증강 선택 화면 → 다음 스테이지로 |

---

## 스테이지 구조 (GameManager — 타워 등반 방식)

**전체 흐름**

1. 게임 시작 → 캐릭터 선택 → `GameManager.Init()`
2. `MakeStageTree()` : stageLevel1~15 리스트 중 등록된 층만 수집
3. `SetStageTree()` : 각 층의 리스트에서 랜덤 1개 프리팹 선택 → y값 50씩 올려 배치
4. 플레이어 방 입장 (`StageInCheckPoint` 트리거)
5. 보스 소환 + 플레이어 `PlayerStartPosition` 이동 + 아래 문(botDoor) 잠김
6. 화면 중앙에 보스 이름 잠시 표시
7. 보스 이름 UI 사라짐
8. 보스 인트로 애니메이션 실행 (`IntroTime`)
9. 인트로 종료 후 보스 스테이지 시작 (`First` / BT 시작)
10. 보스 처치 (`Stage.aliveBossCount` == 0) → `Stage.OpenTopDoor()` → 위 문(topDoor) 개방
11. 포션(`RandomHealPoint`) + 보상 아이템(`ResultBoxNeedFix`) 수령 → 증강 3개 선택
12. 다음 층 계단(`NextStageStairs`) 접촉 → `GameManager.NextLevel()`
13. 모든 층 클리어 → 엔딩 (현재 데모: `thankDemoUI` 표시)

**Stage 프리팹 구성** *(MapMakeSetting 씬에서 직접 제작)*

```
Stage (프리팹)
├── 타일맵               룰타일로 제작한 방 레이아웃
├── bossOBJ             해당 방 전용 보스 (BossBase 상속)
├── BossSpawnPoint      보스 시작 위치
├── PlayerSpawnPoint    플레이어 스폰 위치
├── botDoor             아래 문 (입장 후 잠김)
├── topDoor             위 문 (보스 처치 후 개방)
├── StageInCheckPoint   입장 트리거 (보스 소환 + 문 잠금)
├── NextStageStairs     다음 층 이동 트리거
├── RandomHealPoint     포션 랜덤 스폰 (Potion.cs)
└── ResultBoxNeedFix    보상 아이템 스폰 위치 (TODO: Stage.ResultPickPoint)
```

- `stageLevel1~15` 각각에 등장 가능한 보스방 프리팹 등록
- 리스트가 비어있으면 해당 층 스킵
- 실제 등록은 Unity Inspector에서 직접 설정

---

## 플레이어 시스템

### 캐릭터 3종

| 캐릭터 | CharacterClass | 역할 계승 | 프리팹 활성화 |
|---|---|---|---|
| TV | 0 | 스나이퍼 | m_IsActive: 1 (활성) |
| Charlie | 1 | 솔져 (따발총) | m_IsActive: 0 (비활성) ⚠️ |
| KimKilWhan | 2 | 샷건 | m_IsActive: 0 (비활성) ⚠️ |

> ⚠️ Charlie/KimKilWhan은 프리팹 루트가 비활성이라 `Instantiate` 후 `Awake()`가 자동 실행되지 않음  
> → `StartObjectPOOL()` 전에 반드시 `player.SetActive(true)` 먼저 호출 필요

### 스탯 구조 (`Stats` 클래스)

```
total = (basic + added) * coefficient
```

| 필드 | 설명 |
|---|---|
| basic | 기본값 (PlayerSO ScriptableObject에서 로드) |
| added | 증강 등으로 추가되는 고정값 |
| coefficient | 배율 계수 |

**주요 스탯**: ATK, HP, Speed, AtkSpeed, ReloadCoolTime, SkillCoolTime, RollCoolTime, BulletSpread(탄퍼짐), BulletLifeTime(사거리), LaunchVolume(발사수), Critical, AmmoMax, defense

### 액션

이동, 조준사격(`WeaponSystem`), 구르기(Roll), 스킬, 장전, 시즈모드/플래시(증강으로 활성화)

- **이동 반전 시스템**: `isNoramlMove` 플래그로 Move / Move2 인풋액션 전환 (A119 증강 적용 시)

---

## 무기 시스템 (`WeaponSystem`)

### 타입

- `Shooting` : 일반 연사
- `Charging` : 차징

### 속성 플래그

`fire(불)`, `water(물)`, `ice`, `burn`, `gravity`, `Penetrate(관통)`

### 특수 옵션

| 옵션 | 설명 |
|---|---|
| sizeBody | 몸 크기 비례 총알 크기 |
| locator | 위치 추적 |
| sniping | 스나이핑 |
| humanAttackintelligentmissile | 추적탄 |
| canresurrection | 부활 가능 |
| sniperAtkBuff | 스나이퍼 공격 버프 |

### 오브젝트 풀링

- `WeaponSystem.pools` : Inspector에서 tag / prefab / count 설정
- `poolParent` : `BulletPool` GameObject 할당 → 풀링 총알이 자식으로 관리됨
- 총알 SortingOrder : **9** (레이어 규칙 준수)
- 각 캐릭터 전용 총알 프리팹: `Assets/Prefabs/Weapon/BulletTV.prefab`, `BulletCharlie.prefab`, `BulletKimKilWhan.prefab`

---

## 증강(Augment) 시스템

스테이지 클리어 후 3개의 강화 선택지 제공. 티어(1~3) × 직업별/공용으로 분류.

### 코드 체계

| 범위 | 분류 |
|---|---|
| A9xx | 스탯 증강 (공/체/이속/공속/정밀도/쿨감/크리/장탄) |
| A1xx (100~199) | 공용 1티어 특수 증강 |
| A2xx (200~299) | 공용 2티어 특수 증강 |
| A3xx (300~399) | 공용 3티어 특수 증강 |
| A1xxx (1000~1999) | TV 전용 증강 (CharacterClass=0) |
| A2xxx (2000~2999) | Charlie 전용 증강 (CharacterClass=1) |
| A3xxx (3000~3999) | KimKilWhan 전용 증강 (CharacterClass=2) |

> 직업 전용 증강(code ≥ 1000)은 `AugmentManager.AugmentCall()`에서  
> `requiredClass = (code/1000)-1` 계산 후 현재 `CharacterClass`와 비교, 불일치 시 차단

### 호출 방식

```
AugmentManager.AugmentCall(code)
  → SendMessage("A" + code)
    → private void A####() 실행
      → 특수 증강은 player.AddComponent<A####>() 부착
```

### 데이터 소스

`Resources/CSVReader/` (stat1~3, All1~3, TV1~3, Charlie1~3, KimKilWhan1~3)

### 증강 획득 흐름

1. 캐릭터 선택 → `MakeAugmentListManager.MakeLisk()` → 티어별 리스트 생성
2. 스테이지 내 `ResultBoxNeedFix`에 보상 아이템 스폰 (`Stage.ResultSummon`)
3. 플레이어가 아이템 접촉 → `ResultManager`가 3개 랜덤 추출 → UI 표시
4. 선택 → `AugmentManager.AugmentCall(code)` → 즉시 적용

### 관련 스크립트

| 스크립트 | 역할 |
|---|---|
| ResultManager | 선택 화면 전체 관리 |
| ChoiceSlot | 개별 선택 슬롯 |
| MySpecialList | 현재 보유 증강 목록 확인 UI |
| MakeAugmentListManager | 증강 리스트 생성/관리 |
| AugmentManager | 증강 코드 → 실제 효과 적용 |

---

## 보스 시스템 (설계 초안 v1, BossBase 중심)

### 설계 목표
- 확장성: 보스 추가 시 데이터(SO) + 파생 클래스 + BT 에셋만 추가하면 동작
- 모듈성: 이동/공격/패턴/피격/연출을 컴포넌트 단위로 분리
- 안정성: Stage 진입, Intro 연출, 다중 보스 동시 존재, 문 잠금/해제를 일관된 수명주기로 처리

### 핵심 전제
- 한 스테이지에 보스 개체가 여러 개 동시에 존재할 수 있음
- 보스는 StageInCheck 이후 전투 시작 절차를 거쳐야 함
- Intro 동안에는
  1. 무적
  2. BT 정지(wait)
  3. Intro 종료 시점에서 전투 시작 이벤트(First)
  가 반드시 같은 시간축에서 보장되어야 함

---

### Stage 진입 시 보스 전투 시작 수명주기

1. StageInCheck 트리거 진입
2. Stage가 아래 문 잠금 + 보스 오브젝트 활성화 + 플레이어를 PlayerStartPosition으로 이동
3. Stage가 화면 중앙 보스 이름 UI를 짧게 표시
4. 보스 이름 UI 제거 후 BossBase.StatSet() 호출
5. StatSet()에서 Intro 시퀀스 시작
   - StartInvincibilityNSecond(IntroTime + PostIntroDelay)
   - WaitPls(IntroTime + PostIntroDelay)
   - FirstPls(IntroTime + PostIntroDelay)
6. IntroTime + PostIntroDelay 경과 후
   - invincibility = false
   - wait = false (BT 실행 허용)
   - First() 실행 (실전 패턴 시작 훅)

### 보스 Intro 공통 규약 (재사용 템플릿)

- 보스 활성화 직후
  - Stage가 `OnBossActivatedBeforeIntro()` 호출
  - 이 시점에서 파츠 Animator 기본 상태를 잠금(예: `speed=0`)하여 평상시 애니메이션 선재생 방지
- Intro 시작 직전 (`StatSet()` 초반)
  - BossBase가 `OnBeforeIntroStart()` 호출
  - 잠금한 Animator를 해제하고 Intro 재생 시작
- Intro 재생 우선순위
  1. 파츠별 지정 클립(직접 재생)
  2. 지정 상태명 재생
  3. 이름에 `intro`가 포함된 상태명/클립 fallback
  4. 최종 fallback(첫 유효 클립)
- Intro 길이 계산
  - 파츠별 Intro 길이 중 최댓값을 `IntroTime`으로 사용
  - 전투 시작 지연은 `IntroTime + PostIntroDelay`로 통일
- 권장 기본값
  - `PostIntroDelay = 1.0f` (인트로 종료 직후 완충 1초)

ThreeMonkeyBoss 적용 요약
- Eye/Mouse/Ear 파츠 Intro를 각각 재생하고, 가장 긴 Intro 길이 기준으로 전투 시작 시점을 잡음
- Animator 상태가 없어도 Intro 클립만 있으면 Playable 폴백으로 재생됨
- Intro 재생 전 기본 상태 선재생을 방지하기 위해 활성화 직후 Animator를 동결 후, Intro 시작 시 해제

### 신규 보스 Intro 코드 템플릿

아래 템플릿을 복붙해서, 보스별 Intro 연출만 바꾸면 동일 규약으로 동작한다.

```csharp
public class NewBoss : BossBase
{
  [Header("Intro")]
  [SerializeField] private float postIntroDelay = 1f;
  [SerializeField] private Animator introAnimator;
  [SerializeField] private AnimationClip introClip;
  [SerializeField] private string introStateName = "BossIntro";

  public override void OnBossActivatedBeforeIntro()
  {
    // 이름 표시 구간에서 기본 상태가 먼저 재생되지 않도록 잠금
    if (introAnimator != null)
      introAnimator.speed = 0f;
  }

  protected override void OnBeforeIntroStart()
  {
    // Intro 시작 직전에 잠금 해제
    if (introAnimator != null)
      introAnimator.speed = 1f;

    // 1) 상태 재생 우선
    if (introAnimator != null && !string.IsNullOrEmpty(introStateName))
    {
      int hash = Animator.StringToHash(introStateName);
      if (introAnimator.HasState(0, hash))
      {
        introAnimator.Play(hash, 0, 0f);
        introAnimator.Update(0f);
        return;
      }
    }

    // 2) 상태가 없으면 Playable로 클립 직접 재생 (권장 유틸로 분리)
    // PlayClipWithPlayable(introAnimator, introClip);
  }

  protected override float ResolveIntroTime()
  {
    // 클립 길이가 있으면 IntroTime으로 사용
    if (introClip != null)
      return Mathf.Max(0f, introClip.length);

    return base.ResolveIntroTime();
  }

  protected override float ResolvePostIntroDelay()
  {
    // 인트로 종료 후 완충 시간
    return Mathf.Max(0f, postIntroDelay);
  }

  public override void First()
  {
    // 실제 전투 시작 지점 (BT 시작/패턴 시작)
  }
}
```

템플릿 체크리스트
- Intro 클립에 `AnimationEvent` 빈 항목이 없는지 확인
- Intro 길이와 `PostIntroDelay` 합으로 전투 시작 타이밍이 맞는지 확인
- 활성화 직후 기본 상태 선재생이 발생하지 않는지 확인

> PlayerStartPosition의 존재 이유: StageInCheck 직후 플레이어를 방 내부 안전 위치로 보정해,
> Intro 연출 중 문 경계/복도에서 이탈하거나 문 너머로 밀려나는 상황을 방지하기 위함

---

### BossBase 책임 분리 (Base Contract)

BossBase는 아래 공통 계약만 책임지고, 실제 패턴은 파생 보스가 담당한다.

- 생명주기
  - StatSet(): SO 기반 초기화 + Intro 시작
  - First(): Intro 종료 후 첫 전투 진입 훅 (virtual)
  - BossDie(): 사망 처리 + Stage/GameManager 카운트 반영
- 전투 게이트
  - invincibility: 피격 무효 상태
  - wait: BT/행동 업데이트 정지 상태
- 데이터 주입
  - EnemySO로 스탯/연출시간/카운트 로드

권장: BossBase는 상태 전이만 담당하고, 이동/공격/패턴 선택은 별도 컴포넌트 또는 BT Task로 위임

---

### 데미지 처리 정책 (virtual Damage 활용)

현재 Damage 함수가 virtual인 점을 확장 포인트로 공식화한다.

- 기본 정책
  - BossBase.Damage(float incoming): 기본 감산 로직
- 파생 정책 예시
  - FixedOneDamageBoss: 어떤 공격이든 1 고정 피해
  - PhaseShieldBoss: 특정 페이즈에서 피해 상한/하한 적용
  - AttributeGateBoss: 속성 조건 충족 시에만 유효 피해

권장 패턴
- 데미지 계산을 바로 감산하지 말고 파이프라인으로 분리:
  1. 입력 피해
  2. 방어/상태/페이즈 보정
  3. 최소/최대 클램프
  4. 최종 피해 반영
- 이 파이프라인의 마지막 단계만 Base에서 공통 처리

---

### 다중 보스 동시 존재 설계

문 개방 조건이 보스 단일 객체가 아니라 “스테이지 내 살아있는 보스 수”와 동기화되어야 한다.

- Stage 단위 권장 모델
  - aliveBossCount (스테이지 로컬 카운트)
  - Stage.RegisterBossSpawnCount / Stage.NotifyBossDied 로 증감
  - aliveBossCount == 0 일 때만 topDoor 해제
- GameManager의 전역 bossCount는
  - HUD/글로벌 상태용으로 유지
  - 문 제어에는 관여하지 않음

이유: 동시 다중 보스, 소환 분신, 중간 증원 등에서 전역 카운트 단독 사용 시 문 상태 경쟁 가능

---

### BT + 상속 + SO 결합 전략

역할을 다음처럼 고정한다.

- SO (데이터)
  - 기초 스탯(atk, hp, speed)
  - IntroAnimationTime
  - 보스 카운트 기여값
  - 패턴 파라미터(쿨타임, 사거리, 확률 등)
- BossBase (공통 런타임 계약)
  - 초기화/피격/사망/상태 게이트
  - Intro 동기화
- 파생 Boss 클래스 (보스 고유 규칙)
  - 고유 Damage 정책 override
  - First()에서 시작 패턴 또는 BT 블랙보드 초기화
- Behaviour Tree (의사결정)
  - wait == false && live == true일 때만 Tick
  - 상태 전환/패턴 선택/스킬 실행 시퀀스

핵심 원칙
- 수치와 밸런스는 SO
- 전투 규칙 차이는 파생 클래스
- 순간 의사결정은 BT

---

### Intro 3로직 정합성 체크리스트

StartInvincibilityNSecond(IntroTime + PostIntroDelay), WaitPls(IntroTime + PostIntroDelay), FirstPls(IntroTime + PostIntroDelay) 는
항상 같은 기준 시간으로 동시에 예약되어야 한다.

체크 항목
- Intro 시작 시점에 invincibility=true, wait=true가 즉시 반영되는가
- Intro + PostDelay 종료 시점에 invincibility=false, wait=false가 누락 없이 보장되는가
- First()는 정확히 1회만 실행되는가
- StageInCheck 재진입 시 중복 예약/중복 First()가 발생하지 않는가
- 활성화 직후(이름 표시 구간)에 평상시 애니메이션이 먼저 재생되지 않는가
- AnimationClip의 `AnimationEvent`가 비어있지 않은가(functionName 누락 경고 방지)

---

### 모듈화 우선순위 (구현 순서 권장)

1. BossBase 안정화
   - Intro 시퀀스/상태 전이/이벤트 훅 정리
2. Stage-Boss 계약 분리
   - Stage는 문/플레이어 위치/카운트, Boss는 전투 로직만 담당
3. Damage 파이프라인 도입
   - virtual override 정책 적용점 명확화
4. BT 게이트 통합
   - wait/live 플래그로 BT Tick 제어 일원화
5. SO 세분화
   - 보스별 패턴 파라미터 분리로 하드코딩 제거

---

### 현재 구현 상태 메모

- 기반 클래스: [BossBase.cs](Assets/Script/Monster/Boss/ThreeMonkeyBoss/BossBase.cs)
- 스테이지 진입 트리거: [StageInCheckPoint.cs](Assets/Script/Stage/StageParts/StageInCheckPoint.cs)
- 스테이지 제어: [Stage.cs](Assets/Script/Stage/Stage.cs)
- 현재 구현 보스: ThreeMonkeyBoss 계열

### 첨삭 포인트 (다음 회의용)

- Stage 로컬 bossCount와 GameManager 전역 bossCount의 책임 경계
- BossDie에서 컴포넌트만 제거할지(GameObject 잔존) 객체 전체 제거할지
- Intro 종료 타이밍을 Invoke 기반으로 유지할지, 코루틴/타이머 서비스로 통합할지
- Damage virtual 정책을 인터페이스 전략으로 분리할지 여부

---

## 레이어 규칙

| SortingOrder | 내용 |
|---|---|
| 0~1 | 배경 (Background) |
| 2~4 | 적 및 오브젝트 |
| 5~7 | 큰 적의 뒷다리 (원근감) |
| 8 | 플레이어 뒤 |
| **9** | **플레이어 아래 오브젝트 (총알)** |
| **10** | **플레이어** |
| **11** | **플레이어 위 오브젝트** |
| 12 | (여유) |
| 13~15 | 몬스터 2 (플레이어보다 앞) |

### 2.5D 정렬(타일맵 + 동적 오브젝트) 규칙

- **Sorting Layer(렌더 정렬)와 Physics Layer(충돌/판정)는 분리해서 운용한다.**
- Physics Layer(`LayerMask.NameToLayer("Player"/"Boss"/"Wall"...)`)는 기존 그대로 유지하고, 변경하지 않는다.
- Sorting Layer는 아래처럼 사용한다.
  - `World_Static` : 바닥/벽/고정 기물
  - `World_GroundFX` : 장판/데칼/바닥 경고표시(항상 바닥 위, 동적 오브젝트 아래)
  - `World_Dynamic` : 플레이어/보스/몬스터/드랍
  - `World_Overlay` : 수풀/기둥 앞면 등 가림 전용
  - `UI` : UI 전용
- 동적 오브젝트는 루트에 `SortingGroup`을 두고, 루트 y값 기준으로 `sortingOrder`를 갱신한다.
- 플레이어 무기는 루트 정렬값 기준 상대 오프셋으로 처리한다.
  - 위쪽 조준(뒷모습): 무기 뒤(-)
  - 아래쪽 조준(정면): 무기 앞(+)
- 타일맵은 `TilemapRenderer = Individual` 권장, `Transparency Sort Axis = (0,1,0)` 사용.
- 장판류 프리팹에는 `WorldGroundFXSorting`를 붙여 `World_GroundFX / order=0`을 강제한다.
- 타일맵과의 안전 기준:
  - 바닥 타일맵(`World_Static`)보다 항상 위에 보임
  - 플레이어/보스/투사체(`World_Dynamic`)보다 항상 아래에 보임
  - 충돌 판정은 Physics Layer로 분리 관리하므로 렌더 레이어 변경과 독립적으로 동작

### 보스 콜라이더 분리 규칙 (Foot / Hurtbox)

- 보스 콜라이더는 역할을 반드시 분리한다.
  - **Root(Foot) 콜라이더**: 땅 접점/이동 충돌/플레이어 접촉 데미지 전용
  - **Hurtbox(자식 Trigger) 콜라이더**: 총알/스킬 피격 판정 전용
- Hurtbox 보스에서는 **총알 데미지를 Hurtbox에서만** 받는다. Root는 총알 피격을 받지 않는다.
- 공통 코드 규칙:
  - `BossBase` 자식에 `BossHurtbox`가 하나라도 있으면 자동으로 Hurtbox 전용 피격 모드
  - `useSeparateHurtbox = true`를 켜도 동일하게 Hurtbox 전용 피격 모드
  - 같은 총알이 Root+Hurtbox를 동시에 스쳐도 보스당 1회만 데미지 적용(`Bullet.TryMarkBossHit`)
  - Hurtbox Trigger에서 플레이어 접촉 데미지는 발생하지 않음

### Hurtbox 타입 보스 추가 체크리스트

1. 보스 프리팹 Root(`BossBase`)에 이동 충돌용 콜라이더 유지 (`isTrigger=false`)
2. Root 아래에 `Hurtbox` 자식 오브젝트 생성
3. `Hurtbox`에 `Collider2D` 추가 후 `isTrigger=true`
4. `Hurtbox`에 `BossHurtbox` 컴포넌트 추가 (`ownerBoss` 비우면 자동 연결)
5. Root `BossBase.useSeparateHurtbox`를 `true`로 설정  
   (현재는 자식 `BossHurtbox`가 있으면 자동 인식되므로 안전장치 성격)
6. 플레이 테스트 확인
   - Root 접촉: 플레이어 접촉 데미지 O
   - Hurtbox 접촉: 플레이어 접촉 데미지 X
   - 총알 피격: Hurtbox에서만 O, 중복 타격 X

---

## 테스트씬 시스템 (MapMakeSetting)

| 스크립트 | 역할 |
|---|---|
| TestGameManager | 캐릭터 선택 UI → `StartTest()` → 소환/풀 초기화 |
| TestAugmentUI | 증강 디버그 목록 UI (좌상단 토글 버튼 → 섹션별 목록) |
| TestAugmentManager | `Galmuri9 SDF` 한글 폰트 관리 (`TestAugmentUIRoot`에 부착) |

- 섹션 분류: ★ 직업 티어1/2/3, ■ 공용 티어1/2/3
- 행 클릭 → `AugmentManager.AugmentCall(code)`

---

## UI / 렌더링 주의사항

### ScrollView Viewport
- `Mask + Image(Color.clear)` → 스텐실 실패로 콘텐츠 전체 클리핑 버그 발생
- **`RectMask2D` 사용 권장** (Image 없이 RectTransform 경계로 클리핑, 안정적)

### 비활성 상태 레이아웃
- `ContentSizeFitter` / `VerticalLayoutGroup`은 비활성 GameObject에서 계산 안 됨
- 패널 활성화 후 `Canvas.ForceUpdateCanvases()` + `LayoutRebuilder.ForceRebuildLayoutImmediate()` 필요

### SpriteRenderer.color
- Color 채널 범위는 **0~1** (float)
- `new Vector4(255, 255, 255, 0)` → 255배 포화 → **흰색**으로 날아감
- 올바른 사용: `Color.white`, 투명: `new Color(1f, 1f, 1f, 0f)`

---

## 수정 완료 목록

| 파일 | 내용 |
|---|---|
| PlayerTVSkill.cs | `CompulsoryRollEnd()` 무한재귀 수정 |
| Bullet.cs | CancelInvoke, time=0f, Invoke→직접호출 |
| GameManager.cs | `Init()` 실행순서, `StageLevelSet` null 가드 |
| WeaponSystem.cs | `StartObjectPOOL` null 안전처리, SortingOrder=9 |
| PlayerAnimatorController.cs | 구르기 후 총 흰색 버그 수정 (Vector4→Color) |
| AugmentManager.cs | 직업 불일치 증강 차단 로직 추가 |
| TestGameManager.cs | 비활성 프리팹 `SetActive(true)` 처리, BulletPool 부모 생성 |
| MapMakeSetting 씬 | Viewport `Mask` → `RectMask2D` 교체 |

---

## TODO

### 버그 / 미완성
- [ ] `GameManager.MakeStageTree()` : stageLevel2~15가 모두 stageLevel1 추가하는 버그 수정 필요
- [ ] `Stage.ResultSummon()` : ResultPickPoint 연결 로직 TODO 상태
- [ ] A117 (777 증강) : `atkPercent` 스탯 비활성화로 미완성
- [ ] A126 (반사타입) : 주석 처리됨

### 미구현
- [ ] Gold(돈) 시스템 → 상점 연동
- [ ] 이벤트 방 (`isEventRoom`)
- [ ] 상점 방 (`isShopRoom`)
- [ ] A123 (큰힘큰책임 - 팀킬) : 멀티 의존
- [ ] A128 (프렌드 실드 소환형) : Photon.Instantiate 의존
- [ ] 소환형 증강 개념 수정 예정
- [ ] 목숨형 증강 개념 미정

### BT 확장 TODO
- [ ] 보스 공통 BT 실행 인터페이스 추가 (`IBossBrain`: StartBrain/StopBrain/Tick)
- [x] `BossBase`에 BT 진입 훅 추가 (`TickBehaviorTree()`), `wait/live` 게이트와 통합
- [x] `ThreeMonkeyBoss`, `MonkeyPart`의 `Update()` 직접 로직을 BT Tick 기반으로 이관
- [x] Intro 종료(`First`) 시 BT 시작, 사망(`BossDie`) 시 BT 중지 규약 명문화
- [x] 보스별 BT 파라미터를 `EnemySO`에서 주입할 수 있도록 필드/구조 확장
- [x] Stage 단위 `aliveBossCount` 기준으로 문 개방 조건 정리 (전역 `bossCount`와 역할 분리)
- [ ] 신규 보스 템플릿 제작: SO + Boss 파생 클래스 + BT 에셋만으로 동작 검증
- [x] 테스트 체크리스트 작성: Intro 중 BT 정지, Intro 종료 후 정확히 1회 시작, 다중 보스 동시 사망 시 문 상태 검증 (`BossBase` 컨텍스트 메뉴 리포트)

### 구조 개선 검토
- [ ] Charlie/KimKilWhan 프리팹 루트 비활성 상태 — 의도인지 확인 후 정리
- [ ] `MainGameManager` 완전 제거 또는 역할 명문화 (현재 참고/이관용으로 보존)
- [ ] `Resources.Load(CSV)` → Addressables 교체 (현재 보류)
